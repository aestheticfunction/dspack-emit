; @a2ui/markdown-it@0.0.4 declares a stale peer (@a2ui/web_core@^0.9.2) while we pin
; @a2ui/web_core@0.10.3. The mismatch is benign (markdown-it only renders text), so we
; relax peer resolution here to keep `npm install` reproducible.
legacy-peer-deps=true
